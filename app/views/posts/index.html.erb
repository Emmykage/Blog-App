<%
=begin%>
 <p>Here is a list of post for user <%= @user %></p> 
<%
=end%>

<section class="container">

    <div class='top-info flex'>
        <div class='img-div'>
            <img src=<%= @post.author.photo %>>
        </div>
        <div class='details'>
            <span class="username"><%= @post.author.name %></span>
            <span class="info">Number of post: <%= @post.author.posts_counter %></span>
        </div>
    </div>
    <div class="post-container">
        <div class-det>
            <% @post.author.top_three.each do |post| %>
                <%= link_to user_post_path(@user.id, post.id) do  %>
                    <div class="post-div">
                        <span> <%= post.title %> </span>
                        <p><%= post.text%></p>
                        <div class="post-footer">
                            <span>comments: <%= post.comments_counter %></span>
                            <span>likes: <%= post.likes_counter %></span>
                        </div>
                    </div>
               <%=end%>
                    <div class='post-div'>
                        <ul>
                            <% post.top_five.each do | comments | %>
                                <li> <%= comment.user %> : <%= comment.text  %> </li>
                            <%end%>
                        </ul>
                    </div>
            <%end%>
        </div>
    </div>
</section>